services:
  bot:
    build: .
    image: bad-qb-app
    container_name: bad-qb-bot
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
